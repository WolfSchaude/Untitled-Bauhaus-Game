using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PreGen_Events : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
		//Event_Memory.FindInActiveObjectByName("Event_Overview").GetComponent<EventScript>().GenerateList();

	}

    // Update is called once per frame
    void Update()
    {
        
    }
}
